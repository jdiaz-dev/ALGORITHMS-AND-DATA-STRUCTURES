/* 
    CAPITULO 15: arboles
        --un arbol está compuesto de nodos con nodos hijos
        --el primer nodo es llamado nodo raiz
        --en este capitulo varios tipos de arboles como árboles binarios, busqueda de árboles binarios y busqueda binaria de árboles autoequilibrantes

        **Tree Traversal
        --atravesar un array es simple debido a que solo imcrementas el indice hasta llegar hasta el límite del array
        --con los árboles, los punteros de izquierda y derecha tiene que ser seguido en orden a través de cada elemento del árbol
        --Hay varias formas para atravesar el array, la técnicas más populares son:
            1. pre-order traversal(recorrido pre orden)
            2. post-order traversal(recorrido post orden)
            2. in-order traversal(recorrido en orden)
            3. level-order traversal(recorrido por nivel de orden)
    
        **Pre-order Traversal
            --nodo - izquierda - derecha

        **In-Order Traversal
            -- izquierda - raiz - derecha
        
        **Post-Order Traversal
            --izquierda - derecha - raiz

        **Level-Order Traversal
            --el orden es por cada nivel
*/




